clear all;
close all;
clc;

%%%%%
A=[1,2;4,5];
B=inv(A)
C=A+B;
%pi=3.14
% i or j are used for imaginary number
% eps =2.22*10^-16
%inf is infinity same with -inf
%real max is biggest number realmin is smallest number 
%nan is undefined number
%sqrt() square root 
%abs() absolute value 
% exp() exponental value 
%factorial()
%sign() returns if its positive or negative 
%sin() cos() tan() RADIANTS atan() asin() acos() to CONVERT cos(60*pi/180)
%log() 
% ROW vector a=[1,2,3,4,5]
%size(a)= 1X5 returns size of matrix   length(a)=5 returns number of places
%collum vector b=[1;2;3;4]
%TRANSPOSE c=b'
% a=0:pi/6:pi creats a row vector with incriments 


%element by element a.*b  a./b
%max() returns max  min() returns min 
% sort(a) sorts from small to big
%sort(a,'decend') sorts from big to small
%sum(A) sum of all elements 
% dot(a,b) = sum(a.*b)

%matrix a  
% a(1,1) returns what number is in that place 
%a'=transpose 
%inv(a) = a.^-1
%A/B = A.*B^-1
%A\B= A^-2.*B
%ones(2,3) generates a matrix with size 2X3 with all elements being 1 same
%zeros()  rand() random matrix with size of number in ran(1,2)
% eye(3) identity wwith size 3x3
%magic(3)sum each row is 15
%hilb(4)

%plot
%%% -2:.1:2;
%y=t.^2;
%plot(t,y) can do grid on
%linespace(inital value, final value, number of elements 
% x-linespace(0,1,5) = 0,.25,.5,.75,1



%labbbbbbbb 222222222222222222222222222222222
%real z returns real part
imag() returns imaginary part
phase or angle() returns angle
z'= conjugate
